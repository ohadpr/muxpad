node_modules/
dist/
.DS_Store
*.log
.env
.env.local
coverage/
.vite/
playwright-report/
.playwright-mcp/
test-results/
*.tsbuildinfo
.worktrees/
.pi-subagents/
# Scratch screenshots land at the repo ROOT (design mockups, review captures).
# Leading slash on purpose: PNGs that are real assets live inside packages
# (web/public, docs/) and must stay tracked.
/*.png
